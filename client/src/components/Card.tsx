import { NewsCardProps } from "@/types/news";
import { Box } from "@chakra-ui/react";

const Card = ({ title, description, url, image }: NewsCardProps) => {
  return <Box></Box>;
};

export default Card;
